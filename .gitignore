# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/

# Environment / secrets
.env
*.env
!.env.example

# Runtime data & model caches (Docker volumes / local dev)
data/
models/
backend/data/
backend/models/

# IDE / editor / local tooling
.claude/
.idea/
.vscode/
*.swp
# Unrelated JS toolchain that ended up under .kilo/ (some CLI install side
# effect) — not part of this project, never intended to be committed.
.kilo/node_modules/

# Agent-generated scratch docs that were never requested — README is the
# single source of truth for this project, not a second roadmap doc.
docs/PORTFOLIO_ROADMAP.md

# OS
.DS_Store
Thumbs.db
