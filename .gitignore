# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/

# Environment / secrets
.env
*.env
!.env.example

# Runtime data & model caches (Docker volumes / local dev)
data/
models/
backend/data/
backend/models/

# IDE / editor / local tooling
.claude/
.idea/
.vscode/
*.swp
# Kilo — local agent config/tooling, not part of project source
.kilo/
kilo.json

# Agent-generated scratch docs that were never requested — README is the
# single source of truth for this project, not a second roadmap doc.
docs/PORTFOLIO_ROADMAP.md

# OS
.DS_Store
Thumbs.db
